interface:
  display_name: "Triage"
  short_description: "Process issues through triage"
  default_prompt: "Use $triage to process this issue queue through the repo triage states."
policy:
  allow_implicit_invocation: false
